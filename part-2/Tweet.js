const Tweet = (props) => { 
    return(
<div>
            {props.username} 
            
            {props.name}
            
            {props.date}
            
    { props.message } 

   </div>
    )
}